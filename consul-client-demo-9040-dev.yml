server:
  port: 9040
spring:
  application:
    name: consul-client-demo-9040121212ioi12312321
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: ${spring.application.name}-${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}
      healthCheckPath: ${management.context-path}/health
test:
  test_hello_1
